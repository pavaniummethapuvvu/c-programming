include<stdio.h>
void main()
{
	int n,r,num,sum=0;
	printf("enter the three digit number");
	scanf("%d",&n);
	num=n;
	while(n>0)
	{
		r=n%10;
		sum=sum+(r*r*r);
		n=n/10;
	}
	if(sum==num)
	{
		printf("armstrong number");
	}
	else
	{
		printf("not an armstrong number");
	}
}
